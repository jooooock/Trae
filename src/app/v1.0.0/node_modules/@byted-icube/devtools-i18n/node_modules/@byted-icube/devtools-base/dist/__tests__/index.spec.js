"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("../index");
const ILogger_1 = require("../logger/ILogger");
const MutipleLogger_1 = require("../logger/MutipleLogger");
describe('create logger', () => {
    it('create logger', async () => {
        const { logger, slardar, tea } = (0, index_1.initBaseClient)({
            name: 's',
            pageId: '/asd',
            i18nOptions: {},
        });
        expect(logger).toBeDefined();
        expect(slardar).toBeDefined();
        expect(tea).toBeDefined();
        logger.withContext({ name: 'asda' }).debug('msg');
        const localLogger = new MutipleLogger_1.MutipleLogger({}, [
            logger.getLogger(ILogger_1.LoggerType.Output),
            logger.getLogger(ILogger_1.LoggerType.Console),
        ]);
        const remoteLogger = logger.getLogger(ILogger_1.LoggerType.Slardar);
        localLogger.info('msg');
        remoteLogger.info('msg');
    });
});
