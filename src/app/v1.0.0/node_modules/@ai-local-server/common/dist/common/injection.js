import { Container, Injectable, ScopeEnum } from '@artus/injection';
export const container = new Container('common');
export var ObjectFactory;
(function (ObjectFactory) {
    function register(type) {
        container.set({ type });
    }
    ObjectFactory.register = register;
    function getObject(type, ...args) {
        const obj = container.get(type);
        if (obj.init) {
            obj.init(...args);
        }
        return obj;
    }
    ObjectFactory.getObject = getObject;
})(ObjectFactory || (ObjectFactory = {}));
export function SingletonInjectable(constructor) {
    Injectable({ scope: ScopeEnum.SINGLETON })(constructor);
    ObjectFactory.register(constructor);
}
