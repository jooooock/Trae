"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createMethodImplRegistry = createMethodImplRegistry;
const utils_1 = require("../common/utils");
function createMethodImplRegistry() {
    const methodNameMap = {};
    function decorator(...es) {
        return function (_, __, descriptor) {
            es.forEach((e) => (methodNameMap[e] = descriptor.value));
            return descriptor;
        };
    }
    const registry = {
        get: (type) => {
            const method = methodNameMap[type];
            utils_1.Utils.assert(method, `method ${String(type)} not found`);
            return method;
        },
    };
    return [decorator, registry];
}
