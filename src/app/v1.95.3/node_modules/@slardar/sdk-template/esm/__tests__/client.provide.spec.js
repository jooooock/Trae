import test from 'ava';
import { createClient } from '../client';
import { id, createBaseConfigManager, noopBuilder, noopSender } from '../utils';
test('client.provide - reserved name', function (t) {
    var client = createClient({
        builder: noopBuilder,
        createSender: function () { return noopSender; },
        createDefaultConfig: function () { return ({}); },
        validateInitConfig: function () { return true; },
        initConfigNormalizer: id,
        userConfigNormalizer: id,
        createConfigManager: createBaseConfigManager,
    });
    var start = client.start;
    // no effect
    client.provide('start', function () { });
    t.is(client.start, start);
});//# sourceMappingURL=https://trae.private/sourcemaps/424b4bd987c6c6a4cadf1a08da420593cc6bf75a/node_modules/@slardar/sdk-template/esm/__tests__/client.provide.spec.js.map