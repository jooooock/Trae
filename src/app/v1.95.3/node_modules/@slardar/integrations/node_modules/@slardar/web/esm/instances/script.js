import { createBrowserClient } from '../client';
import { getGlobalInstance } from '../util';
var browserClient = createBrowserClient();
var globalInstance = getGlobalInstance();
if (globalInstance) {
    ;
    ['p', 'pp', 'pcErr', 'pcRej'].forEach(function (name) {
        browserClient.provide(name, globalInstance[name]);
    });
}
export default browserClient;//# sourceMappingURL=https://trae.private/sourcemaps/424b4bd987c6c6a4cadf1a08da420593cc6bf75a/node_modules/@slardar/integrations/node_modules/@slardar/web/esm/instances/script.js.map