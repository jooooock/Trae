"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.divide = void 0;
function divide(arr, groupLength) {
    return arr.reduce((acm, cur) => {
        let last = acm[acm.length - 1];
        if (last.length >= groupLength) {
            last = [];
            acm.push(last);
        }
        last.push(cur);
        return acm;
    }, [[]]);
}
exports.divide = divide;
